package server

func (s *Server) route() {
	// s.app.Post("/attendance", middleware.Jwt(), a.AttendancePresentation.AddAttendance)
	// s.app.Put("/attendance", middleware.Jwt(), a.AttendancePresentation.Checkout)

	// s.app.Get("/attendance", middleware.Jwt(), a.AttendancePresentation.FindAllAttendance)
	// s.app.Get("/attendance/today", middleware.Jwt(), a.AttendancePresentation.GetTodayAttendance)
}
